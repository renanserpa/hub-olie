// This component is a duplicate of /ui/Modal.tsx.
// To consolidate without breaking imports, we now re-export the canonical component.
// This file can be safely removed in a future cleanup if all imports are updated.
import Modal from './ui/Modal';
export default Modal;
